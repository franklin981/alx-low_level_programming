#include "main.h"
/**
 * _isupper -> this function ti check if your alphabet is
 * @c: an integer parameter
 * Return: is 0
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
